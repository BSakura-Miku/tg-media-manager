# Optional zero-shot image classification stack.
# Use CPU-only Torch wheels; the NAS does not need CUDA layers.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchaudio==2.3.1+cpu
torchvision==0.18.1+cpu
open-clip-torch>=2.24

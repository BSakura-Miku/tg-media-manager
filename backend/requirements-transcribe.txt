# Optional local speech-to-text stack. Keep this out of the default image to avoid
# pulling heavy model/runtime layers unless transcription is explicitly needed.
faster-whisper>=1.0.3
stable-ts-whisperless==2.19.1
